set lock_timeout = '2s';
set statement_timeout = '60s';
-- Основа (fnd), блок B: журнал пакета fnd_load_log — под триггером аудита каркаса (AC-6).
-- Журнал и так только дополняется (V104), но правило «ни одно изменение fnd-таблиц мимо audit_log»
-- распространяется и на него: строка журнала пишется с актором app.user_id, как и всё остальное.
-- Только expand (AC-3): новых колонок нет, добавляется триггер и строка реестра fnd_audit_tables.
select fnd_audit_enable('fnd_load_log', 'id');
